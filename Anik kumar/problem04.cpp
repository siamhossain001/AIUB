#include <iostream>
#include <string>
#include <iomanip>
#include <cmath>
#include <vector>
#include<algorithm>

using namespace std;

int main() {
    /*3. Write a program that keeps track of the number of copies of each comic book in a collection. 
    The program should ask the user to enter the name and quantity of each comic book, and then use a loop to update the count for each comic book.
    */
    
    
    return 0;
}
